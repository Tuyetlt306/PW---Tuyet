const scores = [85, 90, 78];
const ages = [18, 21, 16, 25];
const words = ['apple', 'banana', 'cherry', 'date'];
//1.1
console.log(scores.every(n => n > 70));
//1.2
console.log(ages.every(n => n > 15));
//1.3
console.log(words.every(n => n.length > 3));